# Claude Code metadata and worktrees (not part of the deployed site)
.claude/

# Auto-generated analysis files from generate_high_yield.py (the rendered PDFs are kept; the .md scratch files are not)
content/*_Analysis.md

# Lecture pipeline working bundles (transcripts, slide text, generated drafts before install)
_pipeline/

# New-semester raw files awaiting ingest (renumbering)
_incoming/
